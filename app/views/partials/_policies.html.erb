<fieldset>
  <legend class="visuallyhidden">Polices and policy areas</legend>
  <label for="edition_policy_content_ids">Policies</label>

  <p class="help-block" id="policy-description">Tag to <a href="https://www.gov.uk/government/policies">policies</a> if your document is about how the government is developing a policy. Documents that should be tagged to policies include consultations, policy papers, research and analysis publications and press releases. <a href="https://www.gov.uk/guidance/how-to-publish-on-gov-uk/creating-and-updating-pages#policy-and-policy-areas-tags">More on when to tag to policies</a>.</p>

  <input name="edition[policy_content_ids][]" type="hidden" value="" />
  <select multiple="multiple" class="chzn-select form-control" data-placeholder="Choose policies…" name="edition[policy_content_ids][]" id="edition_policy_content_ids" aria-describedby="policy-description">
    <option value=""></option>

    <% policies.each do |pol| %>
    <option value="<%= pol['slug'] %>"><%= pol['title'] %></option>
    <% end %>
  </select>

  <label class="required" for="policy-area-ids">Policy Areas<span>*</span></label>

  <p class="help-block" id="policy-area-description">Users subscribed to policy areas will receive an email when new content is published.</p>

  <input name="edition[topic_ids][]" type="hidden" value="" />
  <select multiple="multiple" class="chzn-select form-control" data-placeholder="Choose policy areas…" name="edition[topic_ids][]" id="policy-area-ids" aria-describedby="policy-area-description">
    <option value=""></option>

    <% policy_areas.each do |pol| %>
    <option value="<%= pol['slug'] %>"><%= pol['title'] %></option>
    <% end %>
  </select>
</fieldset>
