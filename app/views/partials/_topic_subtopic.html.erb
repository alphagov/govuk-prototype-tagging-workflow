<fieldset>
  <legend>Topics</legend>

  <p>
    Tag this item if you want it to appear on a <a href="https://insidegovuk.blog.gov.uk/2015/03/16/taking-stock-of-gov-uks-information-architecture/">subtopic page</a>. Items tagged to a <a href="#">curated subtopic</a> will not appear automatically - tag, publish, then contact GDS to have this item added to a curated subtopic.
  </p>

  <div class="suggested-topics">
    <h3>Suggested topics</h3>

    <p>Your organisation usually tags to:</p>

    <ul class="suggested-topics-list">
      <li><a href="#topic-schools-colleges-childrens-services-curriculum-qualifications-item">Curriculum and qualifications</a></li>
      <li><a href="#topic-schools-colleges-childrens-services-early-years-item">Early years</a></li>
      <li><a href="#topic-schools-colleges-childrens-services-school-college-funding-finance-item">School and college funding and finance</a></li>
    </ul>

    <p><a href="#" class="btn btn-default" id="suggested-topics-select">Select these topics</a> or choose more from below.</p>
  </div>

  <div class="panel panel-default tag-explorer" id="tag-explorer">
    <div class="panel-heading">Topics</div>

    <div class="tag-container">
      <div class="list-group tag-explorer-topics-level-1">
        <% topics.each do |topic| %>
        <a href="#<%= topic['base_path'].parameterize %>" class="list-group-item"><%= topic['title'] %></a>
        <% end %>
      </div>
    </div>

    <div class="tag-container">
      <% topics.each do |topic| %>
      <div class="list-group tag-explorer-topics-level-2 js-hidden" id="<%= topic['base_path'].parameterize %>">
        <% topic['links']['children'].each do |subtopic| %>
        <a href="<%= subtopic['base_path'].parameterize %>" class="list-group-item" id="<%= subtopic['base_path'].parameterize %>-item"><%= subtopic['title'] %></a>
        <% end %>
      </div>
      <% end %>
    </div>
  </div>

  <label for="chosen-subtopics">Selected topics</label>

  <select multiple="multiple" class="chzn-select form-control" data-placeholder="No subtopics currently chosen" name="edition[primary_specialist_sector_tag]" id="chosen-subtopics">
    <option value=""></option>
    <% topics.each do |topic| %>
    <optgroup label="<%= topic['title'] %>">
      <% topic['links']['children'].each do |subtopic| %>
      <option value="<%= subtopic['base_path'].parameterize %>"><%= topic['title'] %> &gt; <%= subtopic['title'] %></option>
      <% end %>
    </optgroup>
    <% end %>
  </select>

</fieldset>
